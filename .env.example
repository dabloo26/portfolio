# Optional build-time base path (see vite.config.ts). Default "/" is correct for
# apex domains and https://<user>.github.io/ user sites.
# GitHub Pages project site: VITE_BASE=/<repo-name>/
# VITE_BASE=/
