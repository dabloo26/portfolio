/** Scroll-triggered sections: replay when re-entering viewport; easier to trigger while scrolling. */
export const sectionViewport = {
  once: false,
  amount: 0.2,
  margin: "0px 0px -12% 0px",
} as const;
